root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -ldflags='-checklinkname=0' -o ./tmp/main ."
  bin = "tmp/main"
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["vendor", "node_modules"]

[log]
  time = true
